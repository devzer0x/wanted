"""Data files for the agent's mission knowledge base (missions.json, hud_legend.md, ...).

The files are extracted from public GTA V walkthrough/wiki pages by a research workflow and
checked by a reviewer before landing here. They are DATA, never part of the cached prompt
prefix: `brain/knowledge.py` injects one mission's card into the dynamic context only while
that mission is active.
"""
